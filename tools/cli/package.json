{
  "name": "onlywrite",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "onlywrite": "./dist/cli.mjs"
  },
  "type": "module",
  "scripts": {
    "build": "vp pack src/cli.ts --out-dir dist --format esm --platform node --target node22",
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "better-sqlite3": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
