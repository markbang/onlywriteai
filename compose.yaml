services:
  onlywrite:
    build: .
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - onlywrite-data:/data
      - ./backups:/backups
    restart: unless-stopped

volumes:
  onlywrite-data:
